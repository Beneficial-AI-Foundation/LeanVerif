digraph proverif {
  rankdir=TB;
  node [shape=box, style=rounded];
  syntax -> main;
  destructor -> main;
  pitsyntax -> main;
  pitranslweak -> main;
  stringPlus -> main;
  rules -> main;
  display -> main;
  piauth -> main;
  proswapper -> main;
  pisyntax -> main;
  parsing_helper -> main;
  reduction -> main;
  terms -> main;
  lemma -> main;
  spassout -> main;
  reduction_helper -> main;
  encode_queries -> main;
  param -> main;
  reduction_bipro -> main;
  termsEq -> main;
  history -> main;
  version -> main;
  move_new_let -> main;
  pitransl -> main;
  simplify -> main;
  tsyntax -> main;
  pievent -> main;
  fileprint;
  history -> syntax;
  display -> syntax;
  parsing_helper -> syntax;
  database -> syntax;
  param -> syntax;
  terms -> syntax;
  stringmap;
  menu_helper -> reduction_interact;
  reduction_helper -> reduction_interact;
  parsing_helper -> reduction_interact;
  display_interact -> reduction_interact;
  evaluation_helper -> reduction_interact;
  reduction_bipro -> reduction_interact;
  terms -> reduction_interact;
  termsEq -> destructor;
  display -> destructor;
  parsing_helper -> destructor;
  param -> destructor;
  terms -> destructor;
  stringPlus -> addexpectedtags;
  display -> display_interact;
  param -> display_interact;
  terms -> display_interact;
  funsymbhash;
  termsEq -> evaluation_helper;
  reduction_helper -> evaluation_helper;
  parsing_helper -> evaluation_helper;
  param -> evaluation_helper;
  terms -> evaluation_helper;
  parsing_helper -> noninterf;
  display -> noninterf;
  param -> noninterf;
  terms -> noninterf;
  stringPlus -> pitsyntax;
  termsEq -> pitsyntax;
  display -> pitsyntax;
  stringmap -> pitsyntax;
  reduction_helper -> pitsyntax;
  parsing_helper -> pitsyntax;
  simplify -> pitsyntax;
  database -> pitsyntax;
  param -> pitsyntax;
  terms -> pitsyntax;
  selfun -> pitranslweak;
  termsEq -> pitranslweak;
  display -> pitranslweak;
  reduction_helper -> pitranslweak;
  parsing_helper -> pitranslweak;
  pievent -> pitranslweak;
  database -> pitranslweak;
  param -> pitranslweak;
  terms -> pitranslweak;
  reduction_helper -> convert_repl;
  stringPlus;
  parsing_helper -> termslinks;
  param -> termslinks;
  terms -> termslinks;
  selfun -> rules;
  termsEq -> rules;
  history -> rules;
  display -> rules;
  database -> rules;
  reduction_helper -> rules;
  parsing_helper -> rules;
  weaksecr -> rules;
  param -> rules;
  terms -> rules;
  noninterf -> rules;
  termslinks -> display;
  stringmap -> display;
  parsing_helper -> display;
  param -> display;
  terms -> display;
  selfun -> piauth;
  lemma -> piauth;
  termsEq -> piauth;
  termslinks -> piauth;
  rules -> piauth;
  history -> piauth;
  display -> piauth;
  reduction_helper -> piauth;
  parsing_helper -> piauth;
  pievent -> piauth;
  reduction -> piauth;
  param -> piauth;
  reduction_bipro -> piauth;
  terms -> piauth;
  stringPlus -> proswapper;
  display -> proswapper;
  parsing_helper -> proswapper;
  param -> proswapper;
  terms -> proswapper;
  parsing_helper;
  stringmap -> pisyntax;
  reduction_helper -> pisyntax;
  parsing_helper -> pisyntax;
  database -> pisyntax;
  param -> pisyntax;
  terms -> pisyntax;
  lemma -> reduction;
  termsEq -> reduction;
  termslinks -> reduction;
  rules -> reduction;
  display -> reduction;
  reduction_helper -> reduction;
  parsing_helper -> reduction;
  pievent -> reduction;
  param -> reduction;
  evaluation_helper -> reduction;
  terms -> reduction;
  pvqueue;
  tree;
  parsing_helper -> terms;
  param -> terms;
  funsymbhash -> terms;
  stringPlus -> proveriftotex;
  fileprint -> proveriftotex;
  version -> proveriftotex;
  parsing_helper -> proveriftotex;
  termsEq -> lemma;
  display -> lemma;
  reduction_helper -> lemma;
  parsing_helper -> lemma;
  pievent -> lemma;
  param -> lemma;
  terms -> lemma;
  display -> menu_helper;
  stringmap -> menu_helper;
  reduction_helper -> menu_helper;
  parsing_helper -> menu_helper;
  display_interact -> menu_helper;
  param -> menu_helper;
  evaluation_helper -> menu_helper;
  reduction_bipro -> menu_helper;
  terms -> menu_helper;
  parsing_helper -> spassout;
  display -> spassout;
  param -> spassout;
  terms -> spassout;
  termsEq -> database;
  history -> database;
  display -> database;
  parsing_helper -> database;
  param -> database;
  tree -> database;
  terms -> database;
  parsing_helper -> main_interact;
  version -> main_interact;
  param -> main_interact;
  menu_interact -> main_interact;
  termsEq -> reduction_helper;
  termslinks -> reduction_helper;
  display -> reduction_helper;
  stringmap -> reduction_helper;
  parsing_helper -> reduction_helper;
  param -> reduction_helper;
  terms -> reduction_helper;
  lemma -> encode_queries;
  termsEq -> encode_queries;
  stringmap -> encode_queries;
  reduction_helper -> encode_queries;
  parsing_helper -> encode_queries;
  simplify -> encode_queries;
  pievent -> encode_queries;
  param -> encode_queries;
  terms -> encode_queries;
  stringPlus -> menu_interact;
  pitsyntax -> menu_interact;
  menu_helper -> menu_interact;
  convert_repl -> menu_interact;
  termsEq -> menu_interact;
  display -> menu_interact;
  reduction_interact -> menu_interact;
  destructor -> menu_interact;
  parsing_helper -> menu_interact;
  simplify -> menu_interact;
  param -> menu_interact;
  reduction_bipro -> menu_interact;
  terms -> menu_interact;
  parsing_helper -> param;
  lemma -> reduction_bipro;
  termsEq -> reduction_bipro;
  display -> reduction_bipro;
  reduction_helper -> reduction_bipro;
  parsing_helper -> reduction_bipro;
  pievent -> reduction_bipro;
  param -> reduction_bipro;
  evaluation_helper -> reduction_bipro;
  terms -> reduction_bipro;
  termslinks -> termsEq;
  display -> termsEq;
  parsing_helper -> termsEq;
  funsymbhash -> termsEq;
  pvqueue -> termsEq;
  param -> termsEq;
  terms -> termsEq;
  termsEq -> history;
  termslinks -> history;
  display -> history;
  reduction_helper -> history;
  parsing_helper -> history;
  param -> history;
  terms -> history;
  stringPlus -> analyze;
  version;
  parsing_helper -> move_new_let;
  param -> move_new_let;
  terms -> move_new_let;
  selfun -> pitransl;
  termsEq -> pitransl;
  display -> pitransl;
  reduction_helper -> pitransl;
  parsing_helper -> pitransl;
  pievent -> pitransl;
  funsymbhash -> pitransl;
  database -> pitransl;
  param -> pitransl;
  terms -> pitransl;
  termsEq -> simplify;
  display -> simplify;
  stringmap -> simplify;
  reduction_helper -> simplify;
  parsing_helper -> simplify;
  param -> simplify;
  terms -> simplify;
  parsing_helper -> pievent;
  display -> pievent;
  stringmap -> pievent;
  funsymbhash -> pievent;
  history -> tsyntax;
  display -> tsyntax;
  parsing_helper -> tsyntax;
  database -> tsyntax;
  param -> tsyntax;
  terms -> tsyntax;
  myocamlbuild;
  termsEq -> weaksecr;
  display -> weaksecr;
  parsing_helper -> weaksecr;
  param -> weaksecr;
  terms -> weaksecr;
  termsEq -> selfun;
  display -> selfun;
  database -> selfun;
  parsing_helper -> selfun;
  weaksecr -> selfun;
  param -> selfun;
  terms -> selfun;
  noninterf -> selfun;
}
